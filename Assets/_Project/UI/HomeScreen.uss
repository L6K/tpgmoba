/* ── Variables ─────────────────────────────────────── */
:root {
    --color-bg:           rgb(15, 15, 31);
    --color-nav:          rgb(25, 25, 46);
    --color-panel:        rgb(25, 25, 51);
    --color-panel-header: rgb(35, 35, 66);
    --color-accent:       rgb(46, 107, 242);
    --color-accent-dim:   rgba(46, 107, 242, 0.35);
    --color-text:         rgb(255, 255, 255);
    --color-text-dim:     rgb(180, 200, 255);
    --color-text-muted:   rgb(120, 130, 160);
    --color-online:       rgb(51, 255, 76);
    --color-offline:      rgb(100, 100, 120);
    --color-divider:      rgba(255, 255, 255, 0.12);
    --nav-height:         70px;
    --friend-width:       220px;
}

/* ── Root ──────────────────────────────────────────── */
.root {
    flex-direction: column;
    width: 100%;
    height: 100%;
    background-color: var(--color-bg);
}

/* ── NavBar ────────────────────────────────────────── */
.nav-bar {
    flex-direction: row;
    align-items: center;
    height: var(--nav-height);
    background-color: var(--color-nav);
    padding-left: 20px;
    padding-right: 12px;
}

.logo {
    font-size: 26px;
    -unity-font-style: bold;
    color: var(--color-text);
    width: 160px;
    margin-right: 20px;
}

.nav-tabs {
    flex-direction: row;
    flex-grow: 1;
}

.nav-tab {
    background-color: rgba(0,0,0,0);
    border-width: 0;
    color: var(--color-text-dim);
    font-size: 17px;
    padding: 0 16px;
    height: 70px;
    border-radius: 0;
    border-bottom-width: 0;
    border-bottom-color: var(--color-accent);
    transition: color 0.15s ease;
    cursor: link;
}

.nav-tab:hover {
    color: var(--color-text);
    background-color: rgba(255,255,255,0.05);
}

.nav-tab--active {
    color: var(--color-text);
    border-bottom-width: 3px;
    border-bottom-color: var(--color-accent);
}

/* 区切り線 */
.nav-divider {
    width: 1px;
    height: 32px;
    background-color: var(--color-divider);
    margin-left: 8px;
    margin-right: 8px;
}

.nav-right {
    flex-direction: row;
    align-items: center;
}

.nav-icon-btn {
    background-color: rgba(0,0,0,0);
    border-width: 0;
    color: var(--color-text-muted);
    font-size: 15px;
    padding: 0 12px;
    height: 70px;
    border-radius: 0;
    cursor: link;
}

.nav-icon-btn:hover {
    color: var(--color-text);
    background-color: rgba(255,255,255,0.05);
}

/* ── Profile Button ────────────────────────────────── */
.profile-btn {
    width: 42px;
    height: 42px;
    border-radius: 21px;
    background-color: rgba(0,0,0,0);
    border-width: 2px;
    border-color: var(--color-accent-dim);
    padding: 0;
    margin-left: 4px;
    cursor: link;
    align-items: center;
    justify-content: center;
    transition: border-color 0.15s ease;
}

.profile-btn:hover {
    border-color: var(--color-accent);
    background-color: rgba(46, 107, 242, 0.15);
}

.profile-icon {
    width: 34px;
    height: 34px;
    border-radius: 17px;
    /* プレースホルダー: グラデーション風の背景色 */
    background-color: rgb(60, 80, 160);
    /* 実際のアイコン画像は HomeScreenController から設定 */
}

/* ── Main Area ─────────────────────────────────────── */
.main-area {
    flex-direction: row;
    flex-grow: 1;
}

/* ── Content ───────────────────────────────────────── */
.content {
    flex-grow: 1;
    position: relative;
}

.page {
    position: absolute;
    left: 0; top: 0; right: 0; bottom: 0;
    display: none;
    align-items: center;
    justify-content: center;
}

.page--active {
    display: flex;
}

.page-placeholder {
    font-size: 32px;
    color: var(--color-text-muted);
}

/* ── Game Center Column ────────────────────────────── */
/* アイコン・パーティ枠・ボタンを縦に並べて中央揃え */
.game-center-col {
    flex-direction: column;
    align-items: center;
    justify-content: center;
}

/* ── Game Icon ─────────────────────────────────────── */
.game-icon {
    width: 320px;
    height: 320px;
    background-color: rgb(26, 31, 71);
    border-width: 2px;
    border-color: var(--color-accent-dim);
    border-radius: 4px;
    align-items: center;
    justify-content: center;
    margin-bottom: 24px;
    transition: border-color 0.2s ease;
}

.game-icon:hover {
    border-color: var(--color-accent);
}

.game-icon__title {
    font-size: 46px;
    -unity-font-style: bold;
    color: var(--color-text);
    margin-bottom: 8px;
}

.game-icon__subtitle {
    font-size: 16px;
    color: var(--color-text-dim);
    letter-spacing: 2px;
}

/* ── Party Slots ───────────────────────────────────── */
.party-slots {
    flex-direction: row;
    align-items: center;
    justify-content: center;
    margin-bottom: 20px;
}

.party-slot {
    width: 80px;
    height: 80px;
    border-radius: 40px;
    border-width: 2px;
    align-items: center;
    justify-content: center;
    margin-left: 10px;
    margin-right: 10px;
    flex-direction: column;
}

.party-slot--empty {
    border-color: rgba(255, 255, 255, 0.2);
    background-color: rgba(255, 255, 255, 0.04);
    transition: border-color 0.15s ease, background-color 0.15s ease;
    cursor: link;
}

.party-slot--empty:hover {
    border-color: var(--color-accent);
    background-color: rgba(46, 107, 242, 0.1);
}

.party-slot__plus {
    font-size: 28px;
    color: rgba(255, 255, 255, 0.25);
}

.party-slot--empty:hover .party-slot__plus {
    color: var(--color-accent);
}

.party-slot--self {
    border-color: var(--color-accent);
    background-color: rgb(26, 31, 71);
}

.party-slot__icon {
    width: 48px;
    height: 48px;
    border-radius: 24px;
    background-color: rgb(60, 80, 160);
    margin-bottom: 0;
}

.party-slot__label {
    font-size: 11px;
    color: var(--color-text-dim);
    margin-top: 4px;
    /* party-slot の外側に小さく表示 */
    position: absolute;
    bottom: -18px;
}

/* ── Play Button ───────────────────────────────────── */
.play-btn {
    width: 230px;
    height: 62px;
    background-color: var(--color-accent);
    border-width: 0;
    border-radius: 4px;
    color: var(--color-text);
    font-size: 22px;
    -unity-font-style: bold;
    margin-top: 16px;
    transition: background-color 0.15s ease, scale 0.1s ease;
    cursor: link;
}

.play-btn:hover {
    background-color: rgb(70, 130, 255);
    scale: 1.03 1.03;
}

.play-btn:active {
    background-color: rgb(30, 80, 200);
    scale: 0.97 0.97;
}

/* ── Friend Panel ──────────────────────────────────── */
.friend-panel {
    width: var(--friend-width);
    background-color: var(--color-panel);
    flex-direction: column;
}

.friend-panel__header {
    height: 50px;
    background-color: var(--color-panel-header);
    align-items: center;
    justify-content: center;
}

.friend-panel__title {
    font-size: 16px;
    -unity-font-style: bold;
    color: var(--color-text);
}

.friend-list {
    flex-grow: 1;
    padding: 8px 0;
}

.friend-row {
    flex-direction: row;
    align-items: center;
    height: 44px;
    padding: 0 12px;
}

.friend-row:hover {
    background-color: rgba(255,255,255,0.05);
}

.friend-dot {
    width: 10px;
    height: 10px;
    border-radius: 5px;
    margin-right: 10px;
}

.friend-dot--online  { background-color: var(--color-online); }
.friend-dot--offline { background-color: var(--color-offline); }

.friend-name {
    font-size: 15px;
    color: var(--color-text);
    flex-grow: 1;
}

.friend-name--offline {
    color: var(--color-text-muted);
}
